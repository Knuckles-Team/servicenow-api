[build-system]
requires = [ "setuptools>=80.9.0", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "servicenow-api"
version = "1.6.59"
description = "Python ServiceNow API Wrapper"
readme = "README.md"
classifiers = [ "Development Status :: 5 - Production/Stable", "License :: Public Domain", "Environment :: Console", "Operating System :: POSIX :: Linux", "Programming Language :: Python :: 3",]
requires-python = ">=3.10"
<<<<<<< HEAD
dependencies = [ "agent-utilities>=0.2.31",]
[[project.authors]]
name = "Audel Rouhi"
email = "knucklessg1@gmail.com"

[project.license]
text = "MIT"

[project.optional-dependencies]
mcp = [ "agent-utilities[mcp]>=0.2.31",]
agent = [ "agent-utilities[agent,logfire]>=0.2.31",]
all = [ "agent-utilities[mcp,agent,logfire]>=0.2.31",]
test = [ "pytest", "pytest-asyncio",]
=======
dependencies = [
    "agent-utilities>=0.2.41"]

[project.optional-dependencies]
mcp = [
    "agent-utilities[mcp]>=0.2.41"]

agent = [
    "agent-utilities[agent,logfire]>=0.2.41"]

all = [
    "agent-utilities[mcp,agent,logfire]>=0.2.41"]
>>>>>>> caeb565 (Bump version: 1.6.56 → 1.6.57)

[project.scripts]
servicenow-mcp = "servicenow_api.mcp_server:mcp_server"
servicenow-agent = "servicenow_api.agent_server:agent_server"

[tool.setuptools]
include-package-data = true

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
check_untyped_defs = true

[tool.setuptools.package-data]
servicenow_api = [ "mcp_config.json", "agent_data/**", "skills/**",]

[tool.ruff.lint]
select = [ "E", "F", "I", "UP", "B",]
ignore = [ "E402", "E501", "B008",]

[tool.setuptools.packages.find]
where = [ ".",]
